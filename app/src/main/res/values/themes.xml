<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GuessWhoIsBack" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorPrimary">@color/white</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.GuessWhoIsBack" parent="Base.Theme.GuessWhoIsBack" />

    <style name="myDropDrown" parent="Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu">
        <item name="android:backgroundTint" >@color/white</item>
        <item name="elevation">0dp</item>
        <item name="android:layout">@layout/mylist</item>
        <item name="colorSurface">@color/black</item>
        <item name="backgroundColor">@color/white</item>
    </style>

    <style name="MyText" parent="TextAppearance.AppCompat">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20dp</item>
    </style>


</resources>